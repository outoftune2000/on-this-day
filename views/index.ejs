<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>On This Day - Wikipedia Events</title>
    <script>document.documentElement.setAttribute('data-theme',localStorage.getItem('theme')||window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light');</script>
    <style>:root{--bg:#fff;--text:#212529}[data-theme=dark]{--bg:#1a1a1a;--text:#e9ecef}html,body{background:var(--bg);color:var(--text)}</style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <link href="/styles.css" rel="stylesheet">
</head>
<body>
    <div class="theme-toggle">
        <button id="themeToggle" aria-label="Toggle dark mode">
            <i class="bi bi-moon-fill"></i>
        </button>
    </div>
    
    <div class="container py-5">
        <h1 class="text-center mb-4">On This Day in History</h1>
        
        <div class="card mb-4">
            <div class="card-body">
                <form class="row g-3 justify-content-center align-items-center">
                    <div class="col-auto">
                        <label for="date" class="form-label">Select Date:</label>
                        <input type="date" class="form-control" id="date" name="date" value="<%= currentDate %>">
                    </div>
                    <div class="col-auto">
                        <button type="submit" class="btn btn-primary mt-4">Show Events</button>
                    </div>
                </form>
            </div>
        </div>

        <h2 class="text-center mb-4">Events on <%= formattedDate %></h2>
        
        <div class="timeline">
            <% events.forEach(event => { %>
                <div class="timeline-item">
                    <div class="timeline-badge">
                        <%= event.year %>
                    </div>
                    <div class="timeline-content">
                        <p>
                            <%= event.text %>
                            <% if (event.pages && event.pages.length > 0) { %>
                                <br>
                                <small>
                                    Read more: 
                                    <% event.pages.forEach((page, index) => { %>
                                        <a href="<%= page.content_urls.desktop.page %>" target="_blank"><%= page.title %></a><%= index < event.pages.length - 1 ? ', ' : '' %>
                                    <% }); %>
                                </small>
                            <% } %>
                        </p>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/scripts.js"></script>
</body>
</html>
